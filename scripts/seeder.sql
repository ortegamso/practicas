-- Placeholder for Seeder SQL script
-- This script can be used to populate the database with initial data or test data.

-- Example for roles (if using the roles table from database.sql):
-- INSERT INTO roles (name) VALUES ('admin'), ('user'), ('premium_user'), ('bot_developer');

-- Example for a default admin user (ensure password_hash is generated securely):
-- INSERT INTO users (username, email, password_hash, role_id) VALUES ('admin', 'admin@example.com', 'your_securely_hashed_password', (SELECT id FROM roles WHERE name = 'admin'));

-- Add other seed data as needed for symbols, default settings, etc.
